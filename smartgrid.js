const smartgrid = require('smart-grid');

var smartgrid = require('smart-grid');
 

var settings = {
    outputStyle: 'less',
    columns: 12,
    offset: '30px',
    mobileFirst: false, 
    container: {
        maxWidth: '1170px', 
        fields: '30px'
    },
    


smartgrid('./precss', settings);